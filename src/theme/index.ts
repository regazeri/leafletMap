import { createTheme } from '@mui/material';

const initialTheme = {
  palette: {
    primary: {
      main: '#333996',
      light: '#3c44b126',
    },
    secondary: {
      main: '#f83245',
      light: '#f8324526',
    },
    background: {
      default: '#f4f5fd',
    },
  },
};
export const theme = createTheme(initialTheme);
